# Linux desktop

Opaque decorated window. `writer` on PATH re-execs the GUI with `WRITER_FORCE_GUI=1`. Terminal fallbacks: `$TERMINAL`, `xdg-terminal-exec`, `foot`, `kitty`, `alacritty`, `ghostty`, then the older names. Desktop file: `apps/desktop/src-tauri/linux/writer.desktop`.
